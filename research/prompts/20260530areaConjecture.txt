Area conj: a+b>1인게 2개 이상 있을때 증명방법 
요약: 밖으로 튀어나가는 넓이가 삼각형 하나 넓이보다 많을거같다
f: [0,1]^2 -> [0,1]
f(a,b) = a, b 점들을 포함하는 unit 정삼각형 중에 hexagon과 intersect하는 넓이가 가장 큰 삼각형의 넓이 (삼각형 1개 넓이를 1이라고 생각) 
a_i+b_i+1 = 1 로 constraint 줘서 육각형 둘레에 6개 점 찍었을때
f(a0,b0)+f(a1,b1)+...+f(a5,b5) < 6 일 것 같다

based on this conjecture, build two new modes
mode 1:
name: Max Area
what it does: I can drag values of a, b using the same codes in hull debug mode.
For given values of a,b, compute f(a,b) numerically and show the triangle that realizes f(a,b). also show 1-f(a,b)

mode 2:
name: Area Conj
what it does: I can drag 6 points on hexagon boundary just like ab union mode.
I have same tools as in ab union mode. move, add, delete, d mark, s mark, f mark
but i don't compute any enclosing triangle.
the left side shows the triangles that realizes each f(ai,bi)
we can do same a_i, b_i, same a_i+b_i all same as ab union mode.
and right panel also shows each f(ai,bi) and 1-f(ai,bi) and each sum.

when implementing, reuse existing functions as much as you can.
if a function is used in different tools, then make a code file that contains does util or geometry functions, and import from them.

think and talk in english.
suggest additional things that might need.
ask me questions to make everything rigorous

in the max area mode,
add checkbox to constraint a+b=1, and checkbox for constraint a+b=1+delta. we can adjust value of 0< delta < 0.16
also add the same checkboxes for ab union and Area Conj panel on each row for each triangle.
ask me questions

for computation issues, when we add constraint a+b=1, actually make it a+b=1 - tolerance. default tolerance 1e-9.
don't make it interface, make it variable hard coded

make tolerance 1e-4

when a+b<=1, when finding the T(a,b) triangle, always try axis aligned triangles.
I see some computational issues and the max area mode doesn't find the correct axis aligned trianlge.
T(a,b) 를 f(a,b)를 realize하는 unit 정삼각형이라고 할때
T(a,b) = 
a+b<=1 일때: axis aligned 삼각형, WLOG a<=b라고 하면 b 점이 위치한 육각형 edge와 삼각형 한 변이 같은 직선
a+b>1 일떄: 삼각형의 한 꼭짓점이 a점 또는 b점 위에 있음 (예전 이름으로는 Type 2 삼각형)
this is my conjecture.
then make tolerance 1e-6.
think and talk in english.
ask me questions.

change default delta to 1e-6
change a+b=1 tolerance to 1e-12